# Home Assistant test configuration for Omada Open API integration

default_config:

logger:
  default: info
  logs:
    custom_components.omada_open_api: debug

# Note: omada_open_api is a config flow integration
# It must be set up through the UI (Settings -> Devices & Services -> Add Integration)
# and cannot be configured via YAML
